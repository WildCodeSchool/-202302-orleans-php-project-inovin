{{ form_start(form) }}
{{ form_widget(form) }}
<div class="d-flex flex-sm-row flex-column justify-content-between my-4">
	<button class="order-2 btn btn-sm bg-primary-light text-dark fw-bolder mb-4">
		{% include "components/svg/_icon_validate.svg.twig" %}
		{{ button_label|default('Valider') }}
	</button>
	<a class="order-3 order-sm-1 btn btn-md mb-4" href="{{ path('app_admin_recipe_index') }}">
		{% include "components/svg/_icon_backlink.svg.twig" %}
		Retour
	</a>
</div>
{{ form_end(form) }}