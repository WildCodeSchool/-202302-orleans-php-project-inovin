{% extends 'base.html.twig' %}

{% block title %}GrapeVariety
{% endblock %}

{% block body %}
	<h1>GrapeVariety</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ grape_variety.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ grape_variety.name }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_cepage_index') }}">back to list</a>

	<a href="{{ path('app_admin_cepage_edit', {'id': grape_variety.id}) }}">edit</a>

	{{ include('admin/grape_variety/_delete_form.html.twig') }}
{% endblock %}