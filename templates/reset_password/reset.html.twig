{% extends 'visitor.html.twig' %}

{% block title %}
	{{ parent()}}
	- Modifier votre mot de passe
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('resetPassword') }}
{% endblock %}

{% block body %}
	<main class="container-fluid d-flex justify-content-center align-items-center my-5 my-md-0">
		<div class="row">
			<div class="col-11 col-md-12 custom-max-width m-auto px-3 px-md-5 border border-secondary d-flex flex-column align-items-center py-5 bg-card ornement position-relative">
				<h2 class="text-center text-uppercase letter-spacing mt-5">Réinitialiser votre mot de passe</h2>
				{% include "components/svg/_frame_surlignage.svg.twig" %}

				<div class="w-100 mt-5">
					{{ form_start(resetForm) }}
					{{ form_row(resetForm.plainPassword) }}
					<div class="d-flex justify-content-center align-items-center">
						<button class="btn-sm order-2 bg-primary-light text-dark fw-bolder icon-link icon-link-hover icon-dark text-uppercase order-2 btn my-5">
							{{ button_label|default('Envoyer') }}
							{% include "components/svg/_icon_next.svg.twig" %}
						</button>
					</div>
					{{ form_end(resetForm) }}
				</div>
			</div>
		</div>
	</main>
{% endblock %}
